# AG Launcher 2.8.0-test.5

This is a focused end-to-end verification release for readable launcher update notes.

## What to verify

- Headings appear as readable text without HTML tags.
- List entries appear as separate bullet points.
- Inline labels such as `Settings → Updates` remain legible.
- No `<h1>`, `<p>`, `<ul>`, `<li>`, or `<code>` markup is visible.

There are no functional launcher changes beyond the version and verification notes. Test releases remain unsigned while Windows Public Trust signing is prepared.
